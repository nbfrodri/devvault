/* eslint-disable @typescript-eslint/no-explicit-any */
export const authComponents: any[] = [
  {
    name: "Glass Auth Form",
    category: "Authentication",
    tags: ["Login", "Register", "Auth", "Framer Motion"],
    rawFolderId: "uncategorized",
    rawEnvironmentId: null,
    dependencies: {
      "framer-motion": "^10.16.0",
      "lucide-react": "^0.300.0",
    },
    code: 'import React, { useState } from \'react\';\nimport { motion, AnimatePresence } from \'framer-motion\';\nimport { Mail, Lock, User, ArrowRight } from \'lucide-react\';\n\nexport default function AuthForm() {\n  const [isLogin, setIsLogin] = useState(true);\n\n  return (\n    <div className="min-h-[550px] bg-[#09090b] flex items-center justify-center p-4">\n      <div className="w-full max-w-md overflow-hidden rounded-[2.5rem] bg-zinc-950 p-8 shadow-2xl border border-white/10 relative">\n        {/* Decorative elements */}\n        <div className="absolute -top-32 -right-32 h-64 w-64 rounded-full bg-indigo-500/20 blur-[80px]" />\n        <div className="absolute -bottom-32 -left-32 h-64 w-64 rounded-full bg-rose-500/20 blur-[80px]" />\n\n        <div className="relative z-10">\n          <div className="flex items-center justify-between mb-8">\n            <h2 className="text-3xl font-bold text-white tracking-tight">\n              <AnimatePresence mode="wait">\n                <motion.span\n                  key={isLogin ? \'login\' : \'register\'}\n                  initial={{ opacity: 0, y: -10 }}\n                  animate={{ opacity: 1, y: 0 }}\n                  exit={{ opacity: 0, y: 10 }}\n                  transition={{ duration: 0.2 }}\n                  className="block"\n                >\n                  {isLogin ? \'Welcome back\' : \'Create account\'}\n                </motion.span>\n              </AnimatePresence>\n            </h2>\n          </div>\n\n          <div className="flex rounded-full bg-white/5 p-1 mb-8">\n            <button\n              onClick={() => setIsLogin(true)}\n              className={`relative flex-1 rounded-full py-2 text-sm font-medium transition-colors ${isLogin ? \'text-white\' : \'text-zinc-500 hover:text-white\'}`}\n            >\n              {isLogin && (\n                <motion.div\n                  layoutId="auth-tab"\n                  className="absolute inset-0 rounded-full bg-white/10"\n                  transition={{ type: "spring", bounce: 0.2, duration: 0.6 }}\n                />\n              )}\n              <span className="relative z-10">Log in</span>\n            </button>\n            <button\n              onClick={() => setIsLogin(false)}\n              className={`relative flex-1 rounded-full py-2 text-sm font-medium transition-colors ${!isLogin ? \'text-white\' : \'text-zinc-500 hover:text-white\'}`}\n            >\n              {!isLogin && (\n                <motion.div\n                  layoutId="auth-tab"\n                  className="absolute inset-0 rounded-full bg-white/10"\n                  transition={{ type: "spring", bounce: 0.2, duration: 0.6 }}\n                />\n              )}\n              <span className="relative z-10">Register</span>\n            </button>\n          </div>\n\n          <form className="space-y-4" onSubmit={(e) => e.preventDefault()}>\n            <AnimatePresence mode="popLayout">\n              {!isLogin && (\n                <motion.div\n                  key="name"\n                  initial={{ opacity: 0, height: 0 }}\n                  animate={{ opacity: 1, height: \'auto\' }}\n                  exit={{ opacity: 0, height: 0 }}\n                  transition={{ duration: 0.3 }}\n                >\n                  <div className="relative flex items-center rounded-2xl border border-white/10 bg-white/5 px-4 py-3">\n                    <User className="mr-3 h-5 w-5 text-zinc-500" />\n                    <input\n                      type="text"\n                      placeholder="Full Name"\n                      className="w-full bg-transparent text-white placeholder:text-zinc-500 focus:outline-none"\n                    />\n                  </div>\n                </motion.div>\n              )}\n              <motion.div\n                key="email"\n                layout\n                className="relative flex items-center rounded-2xl border border-white/10 bg-white/5 px-4 py-3"\n              >\n                <Mail className="mr-3 h-5 w-5 text-zinc-500" />\n                <input\n                  type="email"\n                  placeholder="Email Address"\n                  className="w-full bg-transparent text-white placeholder:text-zinc-500 focus:outline-none"\n                />\n              </motion.div>\n              <motion.div\n                key="password"\n                layout\n                className="relative flex items-center rounded-2xl border border-white/10 bg-white/5 px-4 py-3"\n              >\n                <Lock className="mr-3 h-5 w-5 text-zinc-500" />\n                <input\n                  type="password"\n                  placeholder="Password"\n                  className="w-full bg-transparent text-white placeholder:text-zinc-500 focus:outline-none"\n                />\n              </motion.div>\n            </AnimatePresence>\n\n            {isLogin && (\n              <div className="flex justify-end">\n                <a href="#" className="text-sm text-indigo-400 hover:text-indigo-300 transition-colors mt-2">\n                  Forgot password?\n                </a>\n              </div>\n            )}\n\n            <motion.button\n              layout\n              type="submit"\n              className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-white text-black py-4 font-semibold hover:bg-zinc-200 transition-colors"\n            >\n              {isLogin ? \'Sign In\' : \'Create Account\'} \n              <ArrowRight className="h-4 w-4" />\n            </motion.button>\n          </form>\n        </div>\n      </div>\n    </div>\n  );\n}\n',
  },
];
